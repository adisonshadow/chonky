/**
 * Test adapter layer — re-exports from vitest for generated test files.
 * Import as: import { test, expect, describe } from '@chonky/runtime/test'
 */

export { test, expect, describe, it, beforeAll, beforeEach, afterAll, afterEach } from 'vitest';
